//计算指数
#include<stdio.h>
#include<math.h>
int main()
{
	int n, m;
	scanf("%d", &n);
	m = pow(2, n);
	printf("2^%d = %d",n, m);
}
//计算阶乘和
#include<stdio.h>
int main()
{
	int n;
	scanf("%d", &n);
	int t=1;
	int sum = 0;
	for (int i = 1; i <= n; i++)
	{
		t *= i;
		sum += t;
	}
	printf("%d", sum);
}
